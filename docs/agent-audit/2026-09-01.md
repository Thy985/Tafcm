# Tafcm Daily Audit — 2026-09-01

## 1. Repository State

- Commit: `985033dc`（main）
- Version: v0.1.1+2
- CI: ⚠️ Golden (compare) 最近 4–5 次 merge 持续失败；Analyze/Test/Build/ADI-E2E 绿
- Tests: PASS（`flutter test --exclude-tags golden/perf`）
- Build: PASS（Web + Android Debug APK）

GitHub 状态：0 开放 PR；2 开放 Issue（#215 SnackBar 残留、#216 公式导出空白）。

## 2. Findings

### F-2026-09-01-01
- Category: Potential Bug · Severity: P1 · Confidence: High
- Problem: `svg_to_pdf.dart` 无法渲染真实 MathJax 输出——`<use>` 字形被 `_drawUnsupported` 画成 `[unsupported: ...]` 占位符，导致 PDF 公式无法矢量呈现。
- Evidence:
  - `flutter_app/lib/core/renderers/svg_to_pdf.dart:246`（SvgUse → _drawUnsupported）
  - `flutter_app/assets/mermaid_renderer.html:8`（`fontCache:'global'` → `<defs>`+`<use>` 结构）
  - `flutter_app/lib/core/renderers/svg_parser.dart`（defs 丢弃 / use 不解析引用）
  - 测试缺口：`svg_to_pdf_integration_test.dart` 只用伪造内联 `<path>` SVG，未覆盖真实 `<use>` 结构
- Status: TRACKED_IN_ISSUE（#216，导出公式空白根因之一）

### F-2026-09-01-02
- Category: Potential Bug · Severity: P1 · Confidence: Medium
- Problem: `FormulaRenderHost._OffscreenCapture` 把 `RepaintBoundary` 子树包在 `Opacity(opacity: 0.0)` 里，`toImage()` 疑似合成出全透明 PNG → Word/PDF 嵌入透明图 → 公式空白。
- Evidence:
  - `flutter_app/lib/core/services/formula_pdf_renderer.dart:209-242`
  - Word 导出恒走此 PNG 路径（`word_exporter.dart` preRenderAll）
  - 历史真机验证只断言“不崩溃/字节非空”，从未验证 PNG 像素可见性（`docs/releases/phase3.5-realdevice-issues.md` E2E-P0-8）
- Status: OBSERVED（需真机 PoC 定案：检查导出 PNG alpha 通道）

### F-2026-09-01-03
- Category: Test Gap · Severity: P1 · Confidence: High
- Problem: 没有任何测试验证“导出的公式是可见的”（PNG alpha / 真实 SVG 字形渲染）。
- Evidence: `word_export_semantic_fidelity_test.dart:107-108` 只测 fallback 分支；全测试目录无 alpha/pixel 断言。
- Status: OBSERVED（建议随 #216 修复补回归）

### F-2026-09-01-04
- Category: CI / Regression Risk · Severity: P1 · Confidence: High
- Problem: main 的 Golden (compare) job 最近 4–5 次 merge 持续失败；`flutter_app/test/golden/failures/` 有 112 个 git-tracked 失败产物（该目录本应在 `.gitignore`）。
- Evidence: Actions runs `33403769658`/`33398279483`/`33391709227` failure；`git ls-files .../failures/` = 112；`.gitignore:53`。
- Status: OBSERVED（issue 创建待工具可用后补）

### F-2026-09-01-05
- Category: Docs / Architecture Drift · Severity: P3 · Confidence: High
- Problem: Issue #216 / PR #213 引用 ADR-0032（`docs/decisions/ADR/0032-export-assembly-finite-guarantee.md`），但仓库无此文件（最新 0031）。
- Status: OBSERVED（建议补写 ADR-0032）

### F-2026-09-01-06
- Category: Docs · Severity: P3 · Confidence: High
- Problem: ADR-0022“退役原因”链接指向 `../ADR/0031-rebrand-tafcm.md`（品牌改名），与内容（P0-1 WYSIWYG 修复轮）不符。
- Status: OBSERVED（顺手修正）

## 3. Existing Issue Investigation

### #216 导出的公式渲染不正常（空白/缺失）
- 结论摘要：两个独立缺陷叠加，均以“空白”呈现。详见 `docs/agent-investigations/issue-216-formula-export-blank.md`。
- Root cause：F-2026-09-01-01（SVG `<use>`）+ F-2026-09-01-02（透明 PNG 假设）。
- 下一步：真机 PoC（PNG alpha + logcat 渲染路径）→ 修复 → 补回归。

### #215 导出完成 SnackBar 残留
- 修复已合入（PR #214，`92e6949`），代码验证通过；已添加验证评论，建议关闭。

## 4. Ecosystem Watch

### E-2026-09-01-01 公式渲染三路栈（MathJax-SVG / offscreen-PNG / flutter_math_fork）
- 发现：flutter_math_fork 最新 0.7.4（约 15 个月前发布），是无人维护 flutter_math 的 fork，仍是 Flutter 生态事实标准。
- 评估：三路渲染输出不一致，维护面×3；统一渲染可去 WebView 依赖但需自研 ui.Picture→PDF 桥接。
- 结论：**INVESTIGATE**（小型 PoC），暂不迁移；先修 F-01/F-02。

### E-2026-09-01-02 自研 SVG→PDF 渲染器
- 评估：生态替代 `pw.SvgImage` 仍已知 utf8 边界抛错；自研方案在矢量+CJK+utf8 约束下仍有价值。
- 结论：**KEEP**（补 `<use>` 引用解析即可）。

### E-2026-09-01-03 flutter_inappwebview
- 发现：稳定版 6.1.5 已约 19 个月未动；Tafcm 钉 6.0.0 + platform_interface 1.3.0+1 override。真机离屏 WebView 稳定性是本轮 Export 卡死系列主因。
- 结论：**KEEP**（关注 6.1.x 升级；长期降低 WebView 在导出主链路权重）。

### E-2026-09-01-04 Word 公式 PNG 嵌入（无 OMML）
- 发现：Word 迁移 spike 已判定 docx_creator/docs_gee 因依赖冲突无法集成。
- 结论：**KEEP**（PNG 嵌入是当前合理折中；OMML 列入 backlog）。

## 5. Architecture / Technical Debt

1. 公式渲染三路并行（最高优先技术债）——同源 F-01/F-02。
2. ADR-0032 缺失（F-05）。
3. golden failures/ 混入版本库（F-04）。
4. 无显著分层违规：presentation/domain/core 依赖方向基本符合 ADR-0009/0011。

## 6. Test / Regression Gaps

1. 导出公式“可见性”断言（PNG alpha 非全透明 + 真实 MathJax `<use>` SVG 渲染）。最值得补。
2. 真实 MathJax 输出 fixture（`<defs>`+`<use>`）替换当前伪造内联 path 用例。
3. CI golden 基线校准（stale baseline vs 真实回归）+ 清理 failures/ 入库。

## 7. Recommendations

1. Investigate #216（真机 PoC 验证 F-02；修复 F-01/F-02；补公式可见性回归）。
2. 恢复 main CI 绿色（Golden compare）+ 清理 112 个入库 golden failure PNG。
3. 补录 ADR-0032 + 关闭 #215 + 修正 ADR-0022 链接。

---

*结论：存在真实值得关注的问题。核心为 #216 公式导出空白（F-01 + F-02）与 CI golden 持续红（F-04）。*
